name: Coverage Diff

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  coverage-diff:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v6

      - name: Checkout main into __base
        uses: actions/checkout@v6
        with:
          ref: main
          path: __base

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies (PR)
        run: |
          cd backend && bun install --frozen-lockfile
          cd "$GITHUB_WORKSPACE/mcp-server" && bun install --frozen-lockfile
          cd "$GITHUB_WORKSPACE/packages/shared" && bun install --frozen-lockfile
          cd "$GITHUB_WORKSPACE/gateway" && bun install --frozen-lockfile

      - name: Install dependencies (base)
        run: |
          cd __base/backend && bun install --frozen-lockfile
          cd "$GITHUB_WORKSPACE/__base/mcp-server" && bun install --frozen-lockfile
          if [ -d "$GITHUB_WORKSPACE/__base/packages/shared" ]; then
            cd "$GITHUB_WORKSPACE/__base/packages/shared" && bun install --frozen-lockfile
          fi
          if [ -d "$GITHUB_WORKSPACE/__base/gateway" ]; then
            cd "$GITHUB_WORKSPACE/__base/gateway" && bun install --frozen-lockfile
          fi

      - name: Run tests with coverage (PR - backend)
        run: cd backend && bun test --coverage --coverage-reporter=lcov --coverage-dir=../coverage/pr/backend || true

      - name: Run tests with coverage (PR - mcp-server)
        run: cd mcp-server && bun test --coverage --coverage-reporter=lcov --coverage-dir=../coverage/pr/mcp-server || true

      - name: Run tests with coverage (base - backend)
        run: cd __base/backend && bun test --coverage --coverage-reporter=lcov --coverage-dir=../../coverage/base/backend || true

      - name: Run tests with coverage (base - mcp-server)
        run: cd __base/mcp-server && bun test --coverage --coverage-reporter=lcov --coverage-dir=../../coverage/base/mcp-server || true

      - name: Run tests with coverage (PR - scripts)
        run: cd scripts && bun test --coverage --coverage-reporter=lcov --coverage-dir=../coverage/pr/scripts || true

      - name: Run tests with coverage (base - scripts)
        run: cd __base/scripts && bun test --coverage --coverage-reporter=lcov --coverage-dir=../../coverage/base/scripts || true

      - name: Run tests with coverage (PR - shared)
        run: cd packages/shared && bun test --coverage --coverage-reporter=lcov --coverage-dir=../../coverage/pr/shared || true

      - name: Run tests with coverage (base - shared)
        run: |
          if [ -d __base/packages/shared ]; then
            cd __base/packages/shared && bun test --coverage --coverage-reporter=lcov --coverage-dir=../../coverage/base/shared || true
          fi

      - name: Run tests with coverage (PR - gateway)
        run: cd gateway && bun test --coverage --coverage-reporter=lcov --coverage-dir=../coverage/pr/gateway || true

      - name: Run tests with coverage (base - gateway)
        run: |
          if [ -d __base/gateway ]; then
            cd __base/gateway && bun test --coverage --coverage-reporter=lcov --coverage-dir=../../coverage/base/gateway || true
          fi

      - name: Generate coverage diff
        id: diff
        run: |
          REPORT=$(bun run scripts/coverage-diff.ts \
            "backend:coverage/base/backend/lcov.info:coverage/pr/backend/lcov.info" \
            "mcp-server:coverage/base/mcp-server/lcov.info:coverage/pr/mcp-server/lcov.info" \
            "scripts:coverage/base/scripts/lcov.info:coverage/pr/scripts/lcov.info" \
            "shared:coverage/base/shared/lcov.info:coverage/pr/shared/lcov.info" \
            "gateway:coverage/base/gateway/lcov.info:coverage/pr/gateway/lcov.info")
          echo "report<<EOF" >> "$GITHUB_OUTPUT"
          echo "$REPORT" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Post or update PR comment
        uses: actions/github-script@v7
        with:
          script: |
            const marker = '<!-- coverage-diff -->';
            const body = `${marker}\n${process.env.REPORT}`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c => c.body?.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
        env:
          REPORT: ${{ steps.diff.outputs.report }}
